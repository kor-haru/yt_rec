# yt-rec 작업 규칙

에이전트·기여자가 이 저장소에서 작업할 때 따른다.

## 기본 브랜치

- 기본 브랜치는 `main` 이다. `master` 로 바꾸거나 기본 브랜치 이름을 변경하지 않는다.
- `main` 에 직접 커밋하지 않는다. `main` 체크아웃에서 이슈·PR 구현을 하지 않는다.

## 이슈와 PR

- 이슈·PR 작업은 `main` 과 **별개의 격리된 git 워크트리**에서 한다.
- 그 워크트리의 구현은 **조율 세션이 아니라 별도 에이전트**가 한다.
- 조율 세션은 요구사항·리뷰·이슈 작성·위임만 하고, 구현 파일은 워크트리 에이전트에 맡긴다.
- PR은 관련 이슈를 연결한다 (`Closes #N` 또는 본문 이슈 링크). 이슈 없는 PR을 올리지 않는다.

## GitHub 리뷰

- PR을 검토하면 의견을 **PR 코멘트(또는 리뷰)** 로 남긴다.
- 같은 검토 의견을 **연결된 이슈에도 코멘트**로 남긴다. PR만 남기고 이슈를 비우지 않는다.
- 이슈가 안 붙어 있으면 그 상태로 머지하지 않는다. 이슈를 연결한 뒤에 양쪽 코멘트를 남긴다.
