[project]
name = "yt-rec-push-probe"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = ["PySide6==6.11.1"]

[dependency-groups]
dev = ["pytest>=8,<9"]

[tool.pytest.ini_options]
testpaths = ["."]
addopts = "-q"
